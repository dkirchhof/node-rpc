import { ISerializer } from "../types/serializer";
/**
 * Creates a form data object with param index as key and a json string or file / filelist as value.
 */
export declare const jsonFormDataSerializer: ISerializer;
