import { IDeserializer } from "../types/deserializer";
/**
 * Parses a form data request with param index as key and a json string or file / filelist as value
 */
export declare const jsonFormDataDeserializer: IDeserializer;
